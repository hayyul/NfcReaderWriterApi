# Docker Compose - PostgreSQL (OPTIONAL)
# This project uses Neon PostgreSQL for both local and production.
# This docker-compose.yml is kept for reference if you want to use local PostgreSQL instead.
#
# To use Docker PostgreSQL instead of Neon:
# 1. Uncomment the services below
# 2. Run: docker-compose up -d
# 3. Update .env DATABASE_URL to: postgresql://postgres:postgres123@localhost:5434/gasstation

# services:
#   postgres:
#     image: postgres:15-alpine
#     container_name: gas-station-db
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres123
#       POSTGRES_DB: gasstation
#     ports:
#       - "5434:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   postgres_data:
#     driver: local
